<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.colinelliott.newscratch">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewScratch">
        <activity
            android:name=".RegistryActivity"
            android:exported="false" />
        <activity
            android:name=".LoginRegistry"
            android:exported="false" />
        <activity
            android:name=".LogInHubActivity"
            android:exported="false" />
        <activity android:name=".TherapyActivity" />
        <activity android:name=".ConnectionActivity" /> <!-- <activity android:name=".BaseActivity"></activity> -->
        <!-- <activity android:name=".PreparationActivity" /> -->
        <activity android:name=".ViewPatientActivity" />
        <activity android:name=".CreatePatientActivity" />
        <activity android:name=".AssignExerciseActivity" />
        <activity android:name=".CreateExerciseActivity" />
        <activity android:name=".TherapistActivity" />
        <activity android:name=".HubActivity" />
        <activity android:name=".ui.login.LogInHubActivity" />
        <activity android:name=".ui.login.LoginRegistry" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="@string/title_activity_login" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name="com.wearnotch.service.NotchAndroidService" />
    </application>

</manifest>